sonobuoy-config:
  driver: Job
  plugin-name: azure-arc-app-config-provider
  result-format: junit
spec:
  image: 
  imagePullPolicy: IfNotPresent
  name: plugin
  resources: {}
  volumes:
  - name: results
    emptyDir: {}
  volumeMounts:
  - mountPath: /tmp/results
    name: results